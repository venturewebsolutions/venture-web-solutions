---
import clamp from 'lodash/clamp'

type Props = astroHTML.JSX.HTMLAttributes & {
  level: number
}

const { level, ...props } = Astro.props

const Tag = `h${clamp(level, 1, 6)}` as 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6'
---

<Tag {...props}><slot /></Tag>
